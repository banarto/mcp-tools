[project]
name = "mcp-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pulganova", email = "vizm8.math@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.2",
    "mcp>=2.0.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
mcp-tools = "mcp_tools:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
