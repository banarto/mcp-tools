# (プロジェクト名をここに)

<!-- 1〜2文で、これが何かを説明してください。例:「特定のエージェントに縛られない、MCP(Model Context Protocol)準拠の道具サーバーと、それに接続するクライアントの実装」 -->

## 機能

<!-- 実装した道具・クライアントの機能を列挙してください -->
- サーバー: clock(現在時刻を取得する道具を、MCPプロトコル経由で公開)
- クライアント: サーバーに接続し、Claudeと対話しながら道具を呼び出す(ステップ数上限・コスト上限つき)

## セットアップ

```
uv sync
```

`.env.example` を `.env` にコピーし、実際のAPIキー(`ANTHROPIC_API_KEY`)を設定してください。

## 使い方

<!-- サーバー単体の起動方法(uv run mcpserver.py)と、クライアントからの接続方法(uv run mcpclient.py mcpserver.py)を書いてください -->

## 設計判断

<!--
- claude-agent-from-scratchの中で自作したtoolsの仕組み(手作りのtools一覧・match文での振り分け)と、MCPは何が違うか?
- @mcp.tool()デコレータが、手作りのtools一覧と何を自動化してくれているか?
- なぜ道具をエージェント本体から切り離す設計(MCP)にする価値があると考えたか?
- サーバーとクライアント、それぞれの役割をどう理解したか?実務でクライアントを自作する場面が少ない理由は?
- stdio方式のサーバーでprint()が危険な理由、HTTPとの違い
- uv(パッケージ管理)を採用した理由、pipとの違い、仮想環境の役割
- 公式のクライアントサンプルコードにあった不具合(final_textの位置、コスト計算)を、どう見つけて直したか
-->

## 今後の課題
